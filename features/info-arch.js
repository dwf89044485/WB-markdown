// ============================================================
// INFO-ARCH — 信息架构
// ============================================================
// 内容来源：docs/交互设计说明/02-信息架构.md
// ============================================================

export default {
  id: 'info-arch',
  type: 'feature',
  label: '信息架构',
  anchors: {},
  content: `
    <article class="fp-feature">
      <header class="fp-feature-header">
        <h1>信息架构</h1>
        <p class="fp-subtitle">从四条设计原则推导出对话流的信息架构模型</p>
      </header>

      <blockquote class="fp-lead-quote">
        本文档从四条设计原则出发，推导出对话流的信息架构模型。每一条架构决策都回指向原则，形成从"为什么"到"怎么分"的完整推导链路。
      </blockquote>

      <section>
        <h2>1. 两阶段模型</h2>
        <p>从<strong>原则 1"结果优先，过程可追溯"</strong>推导：同一个 Agent Run，在不同时态下，信息的主次关系不同。</p>

        <pre>阶段一：执行过程态（运行时）
  主角 = 过程信息
  用户最关心：还在推进吗 / 做到哪了 / 出错了吗 / 能不能停

阶段二：结果交付态（完成后）
  主角 = 结果信息
  用户最关心：完成了吗 / 结果是什么 / 交付物在哪 / 下一步做什么</pre>

        <div class="fp-note">
          <strong>核心规则</strong>：运行中，过程浮在前面；完成后，结果浮在前面，过程折叠为追溯入口。
        </div>

        <p>这不是两个页面，而是同一个 Run 容器的两个视图切换——Process View 和 Result View。完成后不删除过程，而是把它从主角降级为可展开的追溯入口。</p>
      </section>

      <section>
        <h2>2. 三区结构</h2>
        <p>从<strong>原则 2"渐进式披露"</strong>推导：不同类型的信息不能混排在同一条信息流里，必须用不同容器收纳。</p>

        <pre>Agent Run
├─ 结果区 Result Area
│   ├─ 最终结论
│   ├─ 交付物（文件、图表、草稿）
│   ├─ 完成 / 部分完成 / 失败状态
│   └─ 下一步操作 + 确认事项
│
├─ 过程区 Process Area
│   ├─ 思考过程 / 任务理解
│   ├─ 执行节点列表
│   ├─ 节点内事件流
│   └─ 异常 / 修复记录
│
└─ 原始区 Raw Area
    ├─ 工具调用详情
    ├─ 命令 + 原始输出
    ├─ 错误原文
    └─ 文件操作记录</pre>

        <h3>三区的隔离规则</h3>
        <table>
          <thead>
            <tr>
              <th>规则</th>
              <th>说明</th>
            </tr>
          </thead>
          <tbody>
            <tr><td>结果区不混入过程</td><td>总结、交付物、方案概要里不放工具调用、命令行、修复过程</td></tr>
            <tr><td>过程区不暴露底层细节</td><td>执行节点只说"搜索了什么""处理了什么"，不把 bash 命令、API 返回值等颗粒度过细的执行细节提前暴露</td></tr>
            <tr><td>原始区不铺在对话流</td><td>底层细节五花八门——命令输出、错误堆栈、文件路径——直接摊开会让对话流视觉秩序崩塌，必须用浮层等独立容器承载</td></tr>
            <tr><td>完成态切换时过程区整体折叠</td><td>不逐条隐藏，而是作为一个可追溯入口整体降级</td></tr>
          </tbody>
        </table>
      </section>

      <section>
        <h2>3. 六级展示层级</h2>
        <p>三区结构解决<strong>横向分区</strong>（什么类型的信息放哪里），六级层级解决<strong>纵向深度</strong>（每个区内部用户能看到多深）。两者叠在一起，构成完整的信息架构。</p>

        <pre>层级           归属区      面向用户          信息密度
─────────────────────────────────────────────────
L0  结果呈现   结果区      结果型用户         低
L1  过程总览   过程区      审核型用户         中低
L2  节点展开   过程区      审核型用户         中
L3  节点事件   过程区      较真型用户         中高
L4  工具详情   原始区      较真型用户         高
L5  原始记录   原始区      Debug 用户        最高</pre>

        <h3>每层承载内容</h3>
        <table>
          <thead>
            <tr>
              <th>层级</th>
              <th>承载内容</th>
              <th>典型示例</th>
            </tr>
          </thead>
          <tbody>
            <tr><td>L0</td><td>任务完成状态、总结、交付物、下一步</td><td>"旅行方案已生成 · 查看 v1.0"</td></tr>
            <tr><td>L1</td><td>执行节点目录、各阶段完成状态</td><td>"理解任务 ✓ · 搜索信息 ✓ · 生成文档 ✓"</td></tr>
            <tr><td>L2</td><td>单节点目标、约束识别、子任务拆分、阶段产出</td><td>"目标：规划关西 7 日 · 约束：父亲膝盖不好…"</td></tr>
            <tr><td>L3</td><td>节点内事件链：搜索了什么、创建了什么、出现了什么异常</td><td>"搜索签证政策 · 处理预算换算 · 文件创建失败已修复"</td></tr>
            <tr><td>L4</td><td>调用了什么技能/工具、执行了什么动作</td><td>"调用技能 docx · 创建文件 generate_plan.js"</td></tr>
            <tr><td>L5</td><td>原始命令、原始输出、错误原文、环境信息</td><td><code>python3 -c "..." → UnicodeDecodeError</code></td></tr>
          </tbody>
        </table>

        <p class="fp-note"><strong>承载介质</strong>：L0–L2 在主对话流内呈现，L3 在节点展开内呈现，L4–L5 用浮层承载——颗粒度越细、种类越杂，越不适合直接铺在对话流里。</p>
      </section>

      <section>
        <h2>4. 语言映射规则</h2>
        <p>从<strong>原则 3"任务语言优先"</strong>推导：不同层级使用不同的语言体系。</p>

        <pre>L0 - L2   任务语言    → 用户能直接理解的任务描述
L3        事件语言    → 介于任务和工具之间的动作描述
L4 - L5   工具语言    → 工具名、命令、原始输出</pre>

        <h3>具体映射示例</h3>
        <table>
          <thead>
            <tr>
              <th>同一件事</th>
              <th>任务语言（L1–L2）</th>
              <th>事件语言（L3）</th>
              <th>工具语言（L4–L5）</th>
            </tr>
          </thead>
          <tbody>
            <tr><td>联网查询</td><td>补充搜索旅行信息</td><td>搜索了签证政策、交通卡详情</td><td><code>web.search query="Kyoto visa 2026"</code></td></tr>
            <tr><td>生成文件</td><td>整合信息输出方案文档</td><td>创建文件、编辑文件、修复编码问题</td><td><code>file_write generate_plan.js</code> → <code>UnicodeDecodeError</code></td></tr>
            <tr><td>图片生成</td><td>生成关西主要地点图片</td><td>生成图片 ×2</td><td><code>image_gen prompt="Arashiyama bamboo..."</code></td></tr>
          </tbody>
        </table>

        <div class="fp-note">
          <strong>核心规则</strong>：用户在外层看到的永远是任务语义，只有主动下钻到 L4–L5 才会看到工具名和原始命令。工具名不是要隐藏，而是放在正确的层级。
        </div>
      </section>

      <section>
        <h2>5. 追溯路径</h2>
        <p>四条架构规则叠合后，形成一条完整的用户追溯路径——从默认态一步步下钻到原始记录：</p>

        <pre>默认态（L0）
  用户看到：总结 + 交付物 + 下一步
  ↓ 点击"执行过程 · 19m40s"

过程总览（L1）
  用户看到：执行节点目录 + 各阶段状态
  ↓ 点击某个节点

节点详情（L2）
  用户看到：该节点的目标、约束、子任务、产出
  ↓ 点击节点内的事件行

节点事件（L3）
  用户看到：这个阶段做了哪些具体动作
  ↓ 点击某个事件

工具详情浮层（L4）
  用户看到：调用了什么技能/工具、执行了什么动作
  ↓ 继续下钻

原始记录浮层（L5）
  用户看到：原始命令 + 输出 + 错误原文
  ↓ 关闭浮层，回到对话流原位置</pre>

        <h3>关键设计规则</h3>
        <ul>
          <li>每一层都有明确的返回路径，不会迷失在深层</li>
          <li>关闭浮层后，对话流位置不跳变</li>
          <li>收起执行过程后，结果区仍然干净前置</li>
          <li>点击已完成的状态行，看到的是该时刻的快照，不累积其他阶段的事件</li>
        </ul>
      </section>
    </article>`,
};
