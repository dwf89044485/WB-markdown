# 移动端 Markdown 规范对比：Ardot 规范 vs Demo 现状

> 对比来源：Ardot「Workbuddy 移动端优化」设计规范 vs `styles/markdown.css` + `engine/markdown.js` 当前实现
> 日期：2026/06/16

---

## 一、整体差异概览

| 维度 | Ardot 规范要求 | Demo 现状 | 差距 |
|------|---------------|-----------|------|
| **H1 支持** | 支持（字号 20px） | **不支持**（parser 无 `# ` 处理） | ❌ 缺失 |
| **H4/H5/H6 支持** | 支持（H4~H6 各有独立间距规则） | **不支持**（仅定义了 h2/h3 样式） | ❌ 缺失 |
| **有序列表 ol** | 支持（等宽字号数字 + 嵌套规则） | **不支持**（parser 只处理 `- ` 无序列表） | ❌ 缺失 |
| **代码块 pre** | 支持（含语言标签+操作按钮） | **不支持**（仅内联 `<code>`，无 pre 块） | ❌ 缺失 |
| **"Search & Think" 组件** | 有独立间距（12px/8px/4px/20px） | **不存在** | ❌ 缺失 |
| **"产物文件" 组件** | 有间距 12px | 有 `.file-card`，但间距不同 | ⚠️ 需对齐 |
| **引用 (blockquote)** | 上下间距 16px | 上下间距 14px/4px 不同 | ⚠️ 需调整 |

---

## 二、详细逐项对比

### 2.1 字体规范

| 元素 | Ardot 规范（Markdown页） | Ardot 规范（文字规则页） | Demo 现状 |
|------|:---:|:---:|:---:|
| **H1 字号** | **20px** | 18px | ❌ 不支持 |
| **H2 字号** | **18px** | 17px | 20px（偏大） |
| **H3 字号** | **16px** | 16px | 16px ✓ |
| **H4 字号** | **16px** | 16px | ❌ 不支持 |
| **H5/H6 字号** | **15px** | 15px | ❌ 不支持 |
| **正文 P** | **15px** | 15px | 15.5px（偏大 0.5px） |
| **表格字号** | **14px** | - | th: 12.5px / td: 13.5px（偏小） |
| **代码块字号** | **14px** | - | ❌ 不支持 |
| **列表字号** | 15px（与正文同） | - | 15px ✓ |

> **权重差异：** Ardot 正文常规字重 **450**，Demo 未指定（默认为 400）。Ardot H2/H3 字重 700，Demo H2 字重 760（偏重）、H3 字重 720。

### 2.2 行高

| 元素 | Ardot 规范（Markdown页） | Ardot 规范（文字规则页） | Demo 现状 |
|------|:---:|:---:|:---:|
| **H1** | 1.25 | 1.5 | ❌ 不支持 |
| **H2** | 1.25 | 1.5 | 1.24 ✓ |
| **H3** | 1.25 | 1.5 | 1.3（接近H3） |
| **H4-H6** | 1.25~1.6 | 1.6 | ❌ 不支持 |
| **正文行高** | 1.6 | 1.6 | 1.68（偏高） |
| **列表行高** | - | - | 1.58 |
| **引用行高** | - | - | 1.55 |

### 2.3 间距对比（核心差距）

| 组合 | Ardot 规范 | Demo 现状 | 对比 |
|------|:---:|:---:|:---:|
| **H1 上/下** | 24px / 20px | ❌ 不支持 | ❌ 缺失 |
| **H2 上/下** | **20px / 16px** | 20px / 10px | ⚠️ 下边距差 6px |
| **H3 上/下** | **16px / 12px** | 18px / 8px | ⚠️ 上边距差 2px，下边距差 4px |
| **H4 上/下** | 16px / 12px | ❌ 不支持 | ❌ 缺失 |
| **H5/H6 上/下** | 16px / 4px | ❌ 不支持 | ❌ 缺失 |
| **P 下边距** | **12px** | 8px | ⚠️ 差 4px |
| **H2 + P 上间距** | 20px（无特殊规则） | md-space-2 = 4px | ❌ Demo 有特殊规则 |
| **H3 + ul 上间距** | - | md-space-2 = 4px | ❌ Demo 有特殊规则 |
| **hr 上/下** | **24px / 24px** | 6px / 20px | ⚠️ 上边距差 18px |
| **引用(blockquote) 上/下** | **16px / 16px** | 14px / 4px | ⚠️ 下边距差 12px |
| **表格 上/下** | **16px / 16px** | 10px / 12px | ⚠️ 上边距差 6px |
| **代码块 上/下** | 16px / 16px | ❌ 不支持 | ❌ 缺失 |
| **列表整体下边距** | - | 12px | |
| **无序列表项 padding** | 16px (一级) | **18px**（p-left） | ⚠️ 差 2px |

### 2.4 功能差距

| 功能 | Ardot 规范 | Demo 现状 | 说明 |
|------|:---:|:---:|:---:|
| **H1 标题** `# ` | ✅ 支持 | ❌ 不支持 | parser 未处理 `# ` |
| **H4/H5/H6 标题** `#### ` / `##### ` / `###### ` | ✅ 支持 | ❌ 不支持 | parser 仅处理 `## ` 和 `### ` |
| **有序列表** `1. ` | ✅ 支持 | ❌ 不支持 | parser 仅处理 `- ` |
| **代码块** `` ``` `` | ✅ 支持（含语言标签+运行/复制/编辑按钮） | ❌ 不支持 | 仅有内联 `<code>` |
| **多级列表嵌套** | ✅ 有详细规范（padding-left 递进） | ❌ 不支持 | 仅支持单层 ul |
| **列表符号规范** | ⚠️ 注明了「不要直接复制符号去开发使用」 | 使用自定义圆点 | ✅ Demo 实现正确 |
| **Table 全屏模式** | ❌ 未提及 | ✅ 有完整实现 | Demo 有额外功能 |
| **类型书写动画** | ❌ 未提及 | ✅ typewriter.js | Demo 有额外功能 |

### 2.5 颜色/视觉差异

| 属性 | Ardot 规范 | Demo 现状 |
|------|:---:|:---:|
| 字体栈 | 未指定 | Inter + PingFang SC 组合 |
| 正文色 | 未指定 | #1c1c1e |
| 标题色 | 未指定 | #111114 |
| 强调色 | 未指定 | #007AFF |
| 引用背景 | 未指定 | rgba(255,159,10,0.10) |
| 引用边框 | 未指定 | #ff9f0a |
| 表格圆角 | 未指定 | 13px |

> Ardot 规范未涉及颜色值定义，Demo 的配色方案可继续沿用。

---

## 三、按优先级排序的修改建议

### P0 — 关键差距（影响内容展示正确性）

| # | 问题 | 影响 | 修改范围 |
|---|------|------|---------|
| 1 | **H1 (`# `) 不支持** | 设计文件中的一级标题无法渲染 | `markdown.js` + `markdown.css` |
| 2 | **H4/H5/H6 不支持** | 四级及以上标题无样式 | `markdown.js` + `markdown.css` |
| 3 | **间距值与规范偏差** | H2下边距差6px, H3上下均差, P下边距差4px | `markdown.css` |

### P1 — 功能缺失

| # | 问题 | 影响 | 修改范围 |
|---|------|------|---------|
| 4 | **有序列表 (ol) 不支持** | 规范中明确要求 | `markdown.js` + `markdown.css` |
| 5 | **代码块 (pre) 不支持** | 规范中明确要求带语言标签 | `markdown.js` + `markdown.css` |
| 6 | **多级列表嵌套** | 规范有 padding-left 递进规则 | `markdown.js` + `markdown.css` |

### P2 — 精细化调整

| # | 问题 | 影响 | 修改范围 |
|---|------|------|---------|
| 7 | **引用 blockquote 下边距** | 规范 16px，Demo 仅 4px | `markdown.css` |
| 8 | **hr 上边距** | 规范 24px，Demo 仅 6px | `markdown.css` |
| 9 | **表格上边距** | 规范 16px，Demo 10px | `markdown.css` |
| 10 | **列表 padding-left** | 规范 16px，Demo 18px | `markdown.css` |
| 11 | **正文字号** | 规范 15px，Demo 15.5px | `markdown.css` |
| 12 | **H2 字号** | 规范 18px，Demo 20px | `markdown.css` |
| 13 | **正文行高** | 规范 1.6，Demo 1.68 | `markdown.css` |

### P3 — 新组件

| # | 问题 | 影响 |
|---|------|------|
| 14 | **"Search & Think" 组件** | 联网搜索/深度思考的间距和展开状态 |
| 15 | **"产物文件" 组件间距对齐** | `.file-card` 间距需调整为 12px |

---

## 四、关键决策点

1. **H2 字号分歧**：Ardot Markdown 规范写的是 **18px**，但 Ardot 文字规则页写的是 **17px**，Demo 当前是 **20px**。建议以 Markdown 页规范 **18px** 为准，因为该页是专门针对 Markdown 渲染的设计。

2. **正文行高分歧**：Demo 使用 1.68 可能是出于更好阅读性的考虑。规范要求 1.6，差距 5%，可以妥协接受或调整。

3. **H2 + p 特殊处理**：Demo 有 `.md h2 + p{margin-top:var(--md-space-2)}`（4px），规范中 H2 下边距 16px + P 上边距 12px，通过「间距取最大值」原则已经是 16px 间距，Demo 的特殊处理会导致 H2 后的段落间距变成 10px（H2下10px + P默认0）。需要评估是否要去掉这个特殊规则。

4. **代码块实现方案**：规范要求代码块有「语言标签」「运行」「复制」「编辑」按钮，需要评估代码块内容是否需要真实的可执行环境，还是仅展示 UI 示意。

---

## 五、总结

当前 Demo 与 Ardot 规范的主要差距集中在：

1. **标题层级不完整**（缺 H1、H4、H5、H6）— 这是最大的结构差距
2. **有序列表和代码块缺失**— 两种规范中明确列出的组件
3. **间距值偏差**— 几乎所有组件的上下间距都需要微调

Demo 在**颜色系统、表格全屏、类型动画**方面有规范未涉及的增强功能，应保留。

如果要完全对齐规范，核心修改集中在：
- `engine/markdown.js` — 增加 H1、H4~H6、ol、pre 的解析
- `styles/markdown.css` — 调整所有间距、字号、行高对应规范值
