# 项目健康检查报告

> 生成日期：2026-06-15
> 作 者：CodeBuddy（你的参谋）

---

## 一、概况

项目当前总计约 **42 个根级条目**（文件/目录），其中核心应用代码约 **300KB**，而无关文件/垃圾数据占用超过 **20MB**。简单说：**核心是健康的，但被大量历史垃圾掩盖了。**

### 核心代码（运行必需，22 个文件）

| 类别 | 大小 | 文件 |
|------|------|------|
| HTML 骨架 | 40KB | `index.html` |
| 剧本数据 | 28KB | `scenario.js` |
| CSS 样式 | 84KB | `styles/` 下 6 个文件 |
| 引擎 JS | 152KB | `engine/` 下 10 个文件 |
| 辅助 JS | ~52KB | `icons-inline.js` + `commit-hash.js` + `design-notes.js` |
| 第三方库 | 48KB | `vendor/Sortable.min.js` |
| 部署配置 | 8KB | `vercel.json` + `vercel-build.sh` |

**合计：约 412KB**，这是项目的血肉。

---

## 二、我的诊断（按严重程度排序）

### 🔴 问题一：`docs/` 目录膨胀（18MB，占整体的 90%）

docs/ 里混入了三类完全不应出现在项目仓库里的东西：

**第一类：AI 对话原始记录（~550KB）**

根目录下 `01-用户原始设计哲学文档.md` 到 `08-动态交互规范文档-v01.md` 这 8 个文件，是你跟 AI 聊设计过程的全量对话记录。它们已经被清理提炼到了 `docs/原则指南/` 和 `docs/交互逻辑/` 里。

- 价值：低。看原始对话不如看提炼版。
- 风险：`01-用户原始设计哲学文档.md` 有 **214KB**，包含大量"好的，明白了"类的来回确认，阅读效率极低。
- 建议：**删除**。或保留一份在本地别处，但从项目仓库清理出去。

**第二类：同一份内容存了 3 种格式（~14.3MB）**

`渐进式披露设计指南` 这份文档同时保存了：
- `渐进式披露设计指南.html`（1MB）—— 网页版
- `渐进式披露设计指南.mhtml`（7.8MB）—— MHTML 打包版
- `渐进式披露设计指南.md`（135KB）—— Markdown 版
- `渐进式披露设计指南_files/`（5.4MB）—— HTML 附属资源

这只是同一份 ChatGPT 对话的 3 份截图。保留 `.md` 版就够了。

**第三类：不相关内容（~8KB）**

`关西旅行搜索结果.md` — 关西旅行搜索结果的原始 dump，跟项目完全无关。

**影响**：18MB 的 docs 拖慢了 `git clone`、`git status`、代码搜索。Vercel 构建脚本虽然在部署时会删掉 docs/，但本地开发每天都要忍受这个噪音。

### 🔴 问题二：目录里跑出了"影子目录"（~60KB）

```
Users/josephdeng/Documents/wb-markdown/
├── engine/player.js     ← 旧版（23KB）
├── engine/sheet.js      ← 旧版
└── styles/markdown.css  ← 旧版
```

这应该是工具/脚本的 bug 导致的：用绝对路径写文件时，在当前目录下生成了嵌套的用户路径。里面是**已经完全过时的旧版代码**。放在那里不仅占空间，还可能让人误以为是当前代码。

### 🟡 问题三：`.agent/` vs `.agents/` — 同一份技能存了两遍

两个目录都通过符号链接指向外部技能目录，内容高度重复（knot-cli、subagent-driven-development），且都已经被 `.codebuddy/settings.json` 取代了。
- `.agent/`（56KB）
- `.agents/`（72KB）

**至少删掉一个。**

### 🟡 问题四：旧备份/缓存堆积

| 条目 | 大小 | 说明 |
|------|------|------|
| `.superpowers/` | 36KB | 6月10日的老 brainstorming session |
| `.gstack/` | 96KB | 浏览器自动化浏览日志（6月12日） |
| `.playwright-cli/` | 20KB | 测试日志和页面快照 |
| `CLAUDE.md.zip` | 4KB | 旧版 CLAUDE.md 备份（已过时，CLAUDE.md 现在是 AGENTS.md 的链接） |
| `preview-sheet-types.png` | 484KB | 大图，是哪个页面预览来着？已不再被引用 |
| `sheet-types-reference.html` | 18KB | 独立参考页，不再链接到主页面 |
| `screenshots/` | 20KB | 截图缓存 |
| `.ardot-cache/` | 12KB | Figma 截图缓存 |
| `.cache/` | 8KB | 空目录 |
| `test-results/` | 4KB | 空的测试结果 |
| `src/` | 12KB | 旧版 Docker 配置，跟根目录 `.with/` 重复 |

### 🟢 问题五（小）：根目录文件太多

当前根目录有 **31 个条目**（排除 docs/、engine/、styles/ 这类正常目录）。虽然不算灾难，但有一些像 `MARKDOWN_STYLE_TOKENS.md`、`design-notes.js` 这样的文件放在根层，缺乏归类。

### 🟢 问题六（小）：`.gitignore` 缺项

当前 `.gitignore` 里有重复条目（`.superpowers/` 写了两遍），也没有涵盖 `screenshots/`、`.playwright-cli/`、`.DS_Store`（虽然 git 可能已经忽略了一些，但明确写明是更好的习惯）。

---

## 三、我的建议（按优先级排列）

### 优先级 A：核弹级清理（腾出 ~98% 的垃圾空间）

#### A1. 重锤 docs/ 目录

**删掉这些**（全部可逆，我会用 `trash` 移到回收站）：

```
渐进式披露设计指南.html          → 释放 1MB
渐进式披露设计指南.mhtml          → 释放 7.8MB
渐进式披露设计指南_files/         → 释放 5.4MB
UI架构与自由度分析.mhtml          → 释放 2.8MB
关西旅行搜索结果.md               → 释放 4.8KB
方案展示与沟通沉淀.md              → 释放 22KB
Agent对话流体验原则指南.md         → 释放 19KB
01.md ~ 08.md（根目录下 8 个）   → 释放 326KB
快速滚动按钮交互逻辑.md            → 释放 553B（3 行文字，可合并到其他文档）
```

**保留这些**（真正有用的参考文档）：

```
docs/交互逻辑/          ← 交互设计"唯一事实来源"
docs/原则指南/           ← 设计原则
docs/sheet交互逻辑/      ← Sheet 交互细则
docs/commit-hash.md     ← hash 显示机制（AGENTS.md 有引用）
docs/ardot-设计系统Token分析.md    ← 设计 token 对照
docs/sheet-content-types.md       ← Sheet 内容类型
docs/快速滚动按钮-实现逻辑.md       ← 快速滚动实现说明
docs/快速滚动按钮-体验逻辑.md       ← 快速滚动体验设计
docs/渐进式披露设计指南.md           ← 保留唯一 MD 版
docs/plans/2026-06-15-design-notes-feature-tabs.md  ← 待执行计划
```

**清理后**：docs/ 从 **18MB / 40+ 文件** 缩小到约 **500KB / ~16 个文件**。

#### A2. 删除 Users/ 影子目录

trash 即可。里面是旧版引擎代码，根据文件大小和时间戳判断，远早于当前版本。

#### A3. 清理缓存/备份垃圾

```bash
trash .superpowers/
trash .playwright-cli/
trash screenshots/
trash .ardot-cache/
trash .cache/
trash test-results/
trash CLAUDE.md.zip
trash preview-sheet-types.png
trash sheet-types-reference.html
trash src/
trash .agent/        # 冗余，保留 .agents/ 即可
```

`.gstack/` 保留或不保留？它是浏览器的浏览日志，96KB。如果你不用 gstack 的 browser 功能了，也可以删。取决于你。

`.env.local` — 已经过期的 Vercel OIDC token（6月10日过期）。如果你不打算本地重新 `vercel login`，可以删掉。

### 优先级 B：优化根目录结构

#### B1. 更新 `.gitignore`

补充缺失规则，去掉重复条目。把 `screenshots/`、`.playwright-cli/`、`.DS_Store` 等加上。

#### B2. 做好后就 commit

把全部清理改动作为一个 commit。commit message 清晰写出删了什么、为什么。

---

## 四、整理后的项目地图（建议）

清理后的项目结构大概长这样，根目录从 31 项降到 ~20 项：

```
wb-markdown/
├── index.html              ← 骨架
├── scenario.js             ← 剧本
├── design-notes.js         ← 交互说明数据
├── commit-hash.js          ← hash 显示
├── icons-inline.js         ← SVG 内联（自动生成）
├── MARKDOWN_STYLE_TOKENS.md ← 样式规范备忘
│
├── engine/                 ← 播放引擎（10 个 JS）
├── styles/                 ← CSS 样式（6 个文件）
├── scripts/                ← 部署脚本
├── docs/                   ← 文档（~500KB，16 个文件）
├── vendor/                 ← 第三方库
├── assets/                 ← 静态资源
│
├── icons/                  ← SVG 源文件
├── .with/                  ← Docker 配置
│
├── .agents/                ← AI 技能配置
├── .codebuddy/             ← CodeBuddy 配置
├── .claude/                ← Claude 配置（可考虑合并）
│
├── .vercel/                ← 部署配置
├── vercel.json
├── vercel-build.sh
│
├── AGENTS.md               ← AI 指令
├── CLAUDE.md → AGENTS.md   ← 符号链接
├── .gitignore
└── 项目健康检查报告.md       ← 本报告（完成使命后可删）
```

---

## 五、执行计划（你点个头我就干）

分 4 步，每步都是独立的 commit：

| 步骤 | 内容 | 影响 |
|------|------|------|
| 1 | 删除 `Users/` 影子目录 + `src/` + 缓存目录 | 清理垃圾 |
| 2 | 清理 docs/ 冗余文件 | 释放 ~17MB |
| 3 | 合并 `.agent/` 到 `.agents/`，删除旧备份 | 减少重复配置 |
| 4 | 更新 `.gitignore`，统一 commit | 长期维护 |

每一步都走 `trash` 而非 `rm`，全部可恢复。删除前会给你看拟删除清单。你确认后我再动手。

---

## 六、一些额外观察（免费的）

### 6.1 AGENTS.md 做得很好

你之前的 AGENTS.md 写得很系统——架构说明、表格交互专项、间距架构约束、Git 工作流、commit-hash 机制等都有了。这份文档本身不需要大改。

### 6.2 引擎架构保持稳定

engine/ 的模块化设计（player.js 作主入口，其他模块各司其职）没有问题。各类之间的依赖关系清晰，没有缠结。**不要动 engine/ 和 styles/ 的结构，它们很健康。**

### 6.3 关于文档的未来

清理之后 docs/ 会小很多。如果将来你想把文档做得更规范（比如用 docusaurus 或 vitepress），现在就有个干净的起点。不过目前来看，纯 markdown 文档对你这项目来说是最合适的——不需要额外构建步骤。

---

**需要你做的决定：**

1. 上面的清理方案你整体认可吗？有没有哪些文件你想保留、不想删？
2. `.gstack/`（96KB 浏览日志）删还是不删？
3. `.env.local`（过期 token）删还是不删？
4. 是让我一次性全做完，还是按步骤分批来？
